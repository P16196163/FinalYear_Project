<UserControl x:Class="CoachAid.TopMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoachAid"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <UserControl.Template>
        <ControlTemplate TargetType="UserControl">

            <!-- Main Background -->
            <Border Background="{StaticResource ForegroundLightBrush}">

                <!-- Content-->
                
                    <!-- Top Menu -->
                    <Grid Grid.Row="0" Background="{StaticResource ForegroundDarkBrush}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Main Buttons-->
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Style="{StaticResource IconButton}" Content="Home"/>
                            <Button Style="{StaticResource IconButton}" Content="Players"/>
                            <Button Style="{StaticResource IconButton}" Content="Team Stats"/>
                            <Button Style="{StaticResource IconButton}" Content="Team Selection"/>
                        </StackPanel>

                    </Grid>
                

            </Border>
        </ControlTemplate>
    </UserControl.Template>
    
</UserControl>
